# 案例教研页面优化计划

## 目标
在保留现有案例教研功能的基础上，设计并实现一个 Vue 可交互演示原型，覆盖管理层与普通教师视角，并新增“一个教研主题—多个话题—多人回复”的教研过程。

## 阶段
- [x] 明确需求与工作方式
- [x] 检查现有线上页面、配色与信息架构
- [x] 产出列表页、编辑/详情页与讨论区视觉概念（图像服务不可用，改以线上截图与设计规格实现）
- [x] 建立 Vue 工程与设计系统
- [x] 实现列表、创建/编辑、发布、话题与回复交互
- [x] 验证桌面端、移动端与核心流程
- [x] 交付说明

## 关键决策
- 技术栈：Vue 3 + Vite + Element Plus（如工作区无既有工程约束）。
- 演示数据全部本地维护，不接真实后台。
- 页面重点展示完整生命周期：准备 → 发布研讨 → 召开/进行中 → 成果沉淀。

## 错误记录
| 错误 | 尝试 | 处理 |
|---|---:|---|
| 初次目录枚举命令返回退出码 1 且无输出 | 1 | 按空目录处理，后续使用更明确的路径检查 |
| 视觉概念生成服务网络错误 | 1 | 保留完整设计规格，缩短请求后重试；如仍失败则按现有页面证据直接实现 |
| `npm install` 长时间无输出 | 1 | 终止等待后检查发现依赖目录已完整生成，改用离线模式补全锁文件并验证构建 |
| 官方 npm 源下载 Element Plus 长时间停滞 | 2 | 切换 npm 镜像后成功完成本地依赖安装 |
| Element Plus ESM 入口缺失导致构建失败 | 1 | 使用 Vite 精确别名指向完整 CommonJS 入口，构建通过 |
| 列表状态标签传入空字符串产生 Vue 警告 | 1 | 改为 `undefined`，热重载后浏览器控制台无应用告警 |
| 热重载使本地标签控制连接短暂断开 | 1 | 复用同一浏览器会话重新获取标签，重载后页面与控制台均正常 |

## 2026-07-30 第二轮细节优化

### 目标
将案例教研流程收敛为“教研准备 → 在线研讨 → 现场记录 → 成果沉淀”，补齐预设话题、成员活跃度、步骤权限、AI 会议纪要、会议资料压缩归档和按教研范围授权，并交付可演示页面与开发需求文档。

### 阶段
- [x] 梳理 11 项新增需求与现有实现差异
- [x] 重构流程节点、状态和角色可见范围
- [x] 实现参与设置中的管理员预设话题
- [x] 将参与成员迁移为侧边抽屉并增加活跃标识
- [x] 实现录音转写后 AI 生成 Markdown 会议纪要
- [x] 将归档报告替换为会议资料压缩包
- [x] 完成素材上传、教研目标和权限说明文案
- [x] 生成并验证需求文档（渲染环境缺失，已完成结构验证）
- [x] 构建、浏览器回归并更新外网演示

### 关键决策
- 普通教师详情仅展示“教研主题、在线研讨、成果文件”三个业务入口；未来节点置灰。
- 参与成员不再作为业务流程 Tab，统一通过右侧成员抽屉查看。
- 离线会议不作为系统流程节点；在线研讨结束后直接进入现场记录。
- 成果仅保留“案例小结、整次教研总结、会议归档包”，不再生成教研报告。
- 前端只演示权限结果，真实权限由后端按“用户 × 教研范围 × 角色”鉴权。

### 错误记录
| 错误 | 尝试 | 处理 |
|---|---:|---|
| 文档标准渲染器未找到 LibreOffice | 1 | 检查工作区运行环境，确认仅提供 PDF 栅格工具 |
| 本机未安装 Microsoft Word COM 组件 | 1 | 不再重复渲染，按文档规范执行 DOCX 结构、样式、表格几何和压缩包完整性检查 |
| PowerShell 不支持 `||` 命令分隔符 | 1 | 改用 `$LASTEXITCODE` 判断检索结果，最终检查通过 |
## 2026-07-31 观察记录 AI 联动

### 目标

在新版“观察与反思 → 观察记录”中补齐两条 AI 业务链路：

1. 选择一篇或多篇观察记录，合并生成一份活动建议，并带入新版“我的教案”双栏编写页。
2. 对单篇观察记录进行家园共育语气润色，预览、编辑后同步至成长档案/成长轶事。

### 阶段

- [x] 查看线上旧版观察记录列表、成长档案同步流程和我的教案字段
- [x] 生成视觉概念并确定页面结构
- [x] 实现侧边栏路由与新版观察记录列表
- [x] 实现 AI 活动建议生成页
- [x] 实现新版我的教案双栏编写页
- [x] 实现 AI 成长轶事生成与同步交互
- [x] 完成桌面端、移动端交互验证和生产构建

### 关键决策

- 批量选择的多篇观察记录只合并生成一份活动建议，避免多份建议割裂。
- 成长轶事按单条观察记录生成，确保幼儿、事实与成长档案归属清楚。
- AI 仅改写和归纳已有事实，不虚构未发生的行为或评价。
- 演示使用本地模拟生成状态，不接入真实后台和 AI 接口。
- 成长档案同步保留旧版的学期、版块、幼儿选择，在提交前增加 AI 草稿预览与编辑。

### 多选成长轶事补充

- [x] 多选操作条增加“AI 生成成长轶事”
- [x] 多篇观察记录按幼儿归组生成草稿
- [x] 弹窗支持切换幼儿、编辑各自草稿和选择同步对象
- [x] 保留单卡快捷生成入口
- [x] 完成构建和浏览器回归验证

决策：跨幼儿的观察事实不得合并成同一篇成长轶事；批量选择涉及多个幼儿时，每名幼儿分别生成一篇草稿。
## 2026-07-31 观察记录三项体验修正

- [x] 核对线上真实成长档案入口、班级/幼儿列表与档案页结构
- [x] 将两个 AI 入口前置为常驻批量操作条，未选择时禁用
- [x] 恢复卡片点击查看详情，选择由独立常显勾选框承担
- [x] 移除成长档案中的“草稿”概念，增加图片与真实档案页面式预览
- [x] 将活动建议收敛为一个可编辑、可复制的大字段
- [x] 教案右侧改为完整建议参考，同时保留活动素材页签
- [x] 构建与浏览器核心路径验证

决策：活动建议作为模板无关的独立正文，不做智能体输出字段与教案模板字段的一一绑定。
