# 教研模块并行工作台优化

## 目标

将教研流程从线性阶段改为“教研主题发布后，教研记录与现场记录并行推进；成果文件集中沉淀”的可交互演示，并完成编辑页与 AI 能力的视觉优化。

## 阶段

- [completed] 盘点现有编辑页、详情页、状态流与素材上传实现。
- [completed] 重构教研详情：补齐顶部“分支—汇合”的并行流程图，并重绘成果沉淀工作台。
- [completed] 优化编辑页：AI 按钮位置、AI 建议大弹窗、素材分组缩略图与预览。
- [completed] 清理预设话题范围，更新“在线研讨”为“教研记录”。
- [completed] 构建、浏览器验证并发布固定演示地址。
- [completed] 统一编辑页与列表页的并行流程表达，消除遗留的线性四阶段组件。
- [completed] 将列表页并行状态重绘为圆弧分支—汇合图，强化并行认知并压缩横向空间。
- [completed] 清除详情页分支框线，使用圆弧连接线表达分流与汇流。
- [completed] 重构详情下半部分为“左侧模块导航 + 主工作区”分栏，并重绘列表的紧凑并行状态图。
- [completed] 建立两级流程导航：顶部切换大流程，左侧展示当前大流程的子步骤并支持锚点定位。
- [completed] 完成列表并行流程图的连线与文字间距视觉校验。
- [completed] 将列表并行流程由拉伸圆弧改为稳定的折线分叉—汇合结构，并完成浏览器视觉校验。
- [completed] 将话题广场与现场记录节点对齐到折线分支区域的水平中轴。
- [completed] 提升列表折线、未完成节点与标签的视觉对比度。

## 验证记录

- `npm run build` 通过（仅保留项目既有的 Rollup 体积提示）。
- 2026-08-10 已发布至 GitHub Pages：`gh-pages` 提交 `3adf690`。
- 浏览器核验首次尝试调用了不存在的 `waitForTimeout`，已改用页面元素等待方式，不重复该调用。
- GitHub Pages 首次推送时远端连接中断；保留已生成提交并单独重试推送后成功。

## 设计决策

- 顶部进度仅表达模块状态，不锁死“教研记录”和“现场记录”的进入顺序。
- 页面主体用单列纵向模块表达每个模块内的待办与完成情况，避免顶部与横向标签重复。
- 素材区按图片、视频、文档、音频分组；保留原文件名，图片/视频显示缩略图。
