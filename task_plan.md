# 教研模块并行工作台优化

## 目标

将教研流程从线性阶段改为“教研主题发布后，教研记录与现场记录并行推进；成果文件集中沉淀”的可交互演示，并完成编辑页与 AI 能力的视觉优化。

## 阶段

- [completed] 基于当前已实现功能与最新澄清，重写详细需求说明书。
- [completed] 校验角色、流程、成果依赖、上传模型、接口与验收标准的一致性。
- [completed] 输出开发交付版 Markdown 文档并完成内容检查。
- [completed] 按用户反馈拆分纯页面版需求，移除接口、数据库和后端实现内容。

- [completed] 盘点现有编辑页、详情页、状态流与素材上传实现。
- [completed] 重构教研详情：补齐顶部“分支—汇合”的并行流程图，并重绘成果沉淀工作台。
- [completed] 优化编辑页：AI 按钮位置、AI 建议大弹窗、素材分组缩略图与预览。
- [completed] 清理预设话题范围，更新“在线研讨”为“教研记录”。
- [completed] 构建、浏览器验证并发布固定演示地址。
- [completed] 统一编辑页与列表页的并行流程表达，消除遗留的线性四阶段组件。
- [completed] 将列表页并行状态重绘为圆弧分支—汇合图，强化并行认知并压缩横向空间。
- [completed] 清除详情页分支框线，使用圆弧连接线表达分流与汇流。
- [completed] 重构详情下半部分为“左侧模块导航 + 主工作区”分栏，并重绘列表的紧凑并行状态图。
- [completed] 建立两级流程导航：顶部切换大流程，左侧展示当前大流程的子步骤并支持锚点定位。
- [completed] 完成列表并行流程图的连线与文字间距视觉校验。
- [completed] 将列表并行流程由拉伸圆弧改为稳定的折线分叉—汇合结构，并完成浏览器视觉校验。
- [completed] 将话题广场与现场记录节点对齐到折线分支区域的水平中轴。
- [completed] 提升列表折线、未完成节点与标签的视觉对比度。
- [completed] 使用低饱和语义色区分准备、话题广场、现场记录和成果节点，保持折线统一色。

## 验证记录

- V2.0 需求说明共 25 章，覆盖角色、非线性流程、页面功能、成果依赖、AI、文件兼容、权限、接口、异常和 50 条验收标准。
- 已检查并排除旧版“案例小结完成后才能生成教研总结”等过时规则。
- 页面版需求共 16 章、52 条页面验收标准，正文不包含接口和技术实现方案。

- `npm run build` 通过（仅保留项目既有的 Rollup 体积提示）。
- 2026-08-10 已发布至 GitHub Pages：`gh-pages` 提交 `3adf690`。
- 浏览器核验首次尝试调用了不存在的 `waitForTimeout`，已改用页面元素等待方式，不重复该调用。
- GitHub Pages 首次推送时远端连接中断；保留已生成提交并单独重试推送后成功。

## 设计决策

- 顶部进度仅表达模块状态，不锁死“教研记录”和“现场记录”的进入顺序。
- 页面主体用单列纵向模块表达每个模块内的待办与完成情况，避免顶部与横向标签重复。
- 素材区按图片、视频、文档、音频分组；保留原文件名，图片/视频显示缩略图。

## 2026-08-18 本轮：组合观察与专题教研

### 目标

- 多条观察合并后形成可保存、可回溯、可再次生成案例的“组合观察”，原始观察保持不变。
- 在案例教研模块内支持“不关联案例”的专题教研；创建、列表、详情和成果依赖保持一致。

### 阶段

- [completed] 审阅线上入口截图、现有原型的观察记录列表、活动建议、教研列表与创建页。
- [completed] 实现观察记录“原始观察 / 组合观察”页签及组合观察保存、查看、生成案例入口。
- [completed] 实现创建教研时“案例教研 / 专题教研”分流，并同步列表数据展示。
- [completed] 实现专题教研详情与成果沉淀：移除关联案例和案例小结，调整自评与归档条件。
- [completed] 更新页面需求说明，补充数据与验收边界。
- [in_progress] 完成构建、桌面与移动端浏览器验证、交互回归和截图（Chrome 扩展已脱离控制，待用户重新打开本地页后继续）。

### 错误记录

- 修改专题教研演示状态时，首次补丁误命中列表第 1 条记录的 `stage/status`；已通过读取完整数据文件发现，并改为按 `id: 5` 上下文精确修正。
- 首次批量更新两份需求说明时，补丁中的“发布预览”上下文与文档的编号格式不一致，整批未写入；已拆成精确上下文补丁完成更新。
- Chrome 扩展连续拦截 `localhost` 和本机局域网地址，返回 `ERR_BLOCKED_BY_CLIENT`；当前不能使用用户指定的 Chrome 对本地原型做渲染验证，未切换到其他浏览器。
- 启动 Vite 时额外传入的 host 参数被 npm 当作普通参数；服务仍成功监听 `0.0.0.0:5173`，后续不重复该启动方式。
- 该错误启动实际把额外参数当成 Vite 根目录，导致首页返回 404；已停止旧进程并用 `npm run dev` 正确重启，HTTP 首页返回 200。
- 服务修正后 Chrome 标签页连续两次在导航时脱离控制；按浏览器恢复规范改用新标签仍相同，因此停止重复尝试，等待用户重新提供本地标签页。

### 本轮设计决策

- 使用“组合观察”而非“合并记录”：强调它是新的可复用对象，不覆盖、拼接或篡改原始记录。
- 生成案例时默认“保存组合观察并生成案例”；同时允许只保存，组合观察列表保留来源数量、时间跨度和案例状态。
- 无案例模式命名为“专题教研”；仍保留在当前模块和同一列表中，以类型标签区分，避免新增平行模块。
- 专题教研成果中不渲染“案例小结”占位；AI 会议自评和归档依赖只计算实际存在的成果。
